spring:
  application.name: function_stored_procedures

# Database configuration
  datasource:
    url: jdbc:mysql://localhost:3306/springDB
    username: admin
    password: admin
    driver-class-name: com.mysql.cj.jdbc.Driver

# Hibernate properties
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
